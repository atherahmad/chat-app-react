{
  "name": "chat-app-react",
  "version": "1.0.0",
  "description": "MERN App with Socket.IO",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": " export NODE_ENV=development && concurrently  --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "prod": "export NODE_ENV=production && concurrently --kill-others \"npm run prod --prefix backend\" \"npm run prod --prefix client\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atherahmad/chat-app-react.git"
  },
  "author": "Ather Ahmad",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/atherahmad/mern-chat-app/issues"
  },
  "homepage": "https://github.com/atherahmad/mern-chat-app#readme",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
